[project]
name = "univai-live"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = []

[project.optional-dependencies]
integrated = [
  "faster-whisper>=1.1",
  "kokoro-onnx>=0.4",
  "livekit-agents~=1.4",
  "livekit-plugins-silero~=1.4",
  "numpy>=1.26",
  "piper-tts>=1.2",
  "psycopg[binary]>=3.2",
  "python-dotenv>=1.0",
]
